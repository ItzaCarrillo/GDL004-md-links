const readLinks = require('./validamdAndReadFile.js');
const validaLinks = require('./validaLinks.js');



const mdLinks = (file, option ) => {
  
}

/*function validateLinks(linkArray){
  let counter0k = 0
  counterErr = 0
  for(let i = 0; i < linkArray.lenght; i++)
  fetch(li[i])
  .then(function(data){
    counter0k++;
  });
  //.catch((err)=>{
   // counterErr++;
 // });
}
*/
